node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Agent-skill adapter mirrors — symlink copies of .agents/skills/ generated
# by `npx skills add` for other AI tools. Only .agents/ is the source of
# truth; regenerate mirrors with: npx remotion skills add
.agent/
.claude/
.codex/
.gemini/
.github/skills/
