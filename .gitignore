# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvsca*
*.njsproj
*.sln
*.sw?

# TypeScript build info
*.tsbuildinfo

# Netlify
.netlify

# Backups/Temporary configs
*.backup

# Environment variables (never commit real values; .env.example is allowed)
.env
.env.*
!.env.example

# Local editor/agent config
.claude/settings.local.json

# GSD runtime state (managed by GSD itself, never committed)
.gsd/gsd.db*
.gsd/runtime/
.gsd-worktrees/
.gsd-backups/

# External skills tool cache — a separate third-party git clone, not
# part of this project's history. Referenced by .agents/skills.json
# via absolute path; keep it on disk, never track/commit it.
github skills/
